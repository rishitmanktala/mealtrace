{
  "name": "mealtrace",
  "version": "1.0.0",
  "private": true,
  "description": "MealTrace nutrition tracker static frontend with a small Node API server.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
